#set($pageTitle="Search: $query")
#parse("header.vm")
#parse("navbar.vm")

#set($target='<nullsearch>')
#if($query && !$query.trim().isEmpty()) #set($target=$query) #end

#set($response=$registry.perform("Search","$uri?query=$lib.pathEncode($query)&_view=with_metadata"))

$response.status
$response.metadata
$response.entity

<h3>Search results for: $query</h3>


#include("footer.vm")
