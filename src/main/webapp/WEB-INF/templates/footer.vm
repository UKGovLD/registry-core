    <hr id="footer-line" />
    <div class="row footer">
     <div class="pull-right">Developed by <a href="http://www.epimorphics.com/">Epimorphics Ltd</a></div>
    </div>
   </div> <!-- End of container -->

   <script type="text/javascript" src="$uiroot/js/jquery-1.8.3.min.js"></script>
   <script type="text/javascript" src="$uiroot/js/bootstrap.min.js"></script>
   <script type="text/javascript">
    $(function() {
        // enable popups
        $(".popinfo").popover();

        // Set up ajax tabs
        $('.action-tab').bind('show', function(e) {
           var pattern=/#.+/gi
           var contentID = e.target.toString().match(pattern)[0];
           var action = $(contentID).attr('data-action')
           $(contentID).load('$uiroot/' + action +'?uri=$lib.pathEncode($uri)', function(){
                $('.action-tab').tab(); //reinitialize tabs, is this needed?
            });
        });
    });

   </script>

  </body>
</html>
